namespace Spellbound.Spells
{
    /// <summary>
    /// The two atomic inputs the entire game is built from.
    /// Tap = short press/release ("."), Hold = press/hold/release ("-").
    /// </summary>
    public enum SpellSymbol
    {
        Tap,
        Hold
    }
}
